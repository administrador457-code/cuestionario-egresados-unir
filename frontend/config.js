// URL del backend. Vacío = mismo servidor que sirve esta página (Railway).
// Si publicas el frontend aparte (por ejemplo en Vercel), pon aquí la URL del
// backend, p. ej.: window.API_BASE = "https://cuestionario-egresados.up.railway.app";
window.API_BASE = "";
